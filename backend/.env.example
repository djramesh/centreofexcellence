# ============================================
# BACKEND ENVIRONMENT VARIABLES (Example)
# ============================================
# Copy this to .env and fill in your actual values
# NEVER commit .env to git!

# Server Configuration
NODE_ENV=development
PORT=4000

# Frontend origin for CORS
FRONTEND_ORIGIN=http://localhost:5173

# MySQL Database Configuration
# For Railway: Use the MySQL connection string provided
DB_HOST=localhost
DB_PORT=3306
DB_USER=root
DB_PASSWORD=your_password_here
DB_NAME=coe_ecommerce

# JWT Authentication (IMPORTANT: Use a strong random string!)
# Generate with: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
JWT_SECRET=your_jwt_secret_here_minimum_32_characters
JWT_EXPIRES_IN=7d

# Razorpay Payment Gateway (Test Mode for now)
# Get from Razorpay Dashboard → Settings → API Keys → Test Mode
RAZORPAY_KEY_ID=rzp_test_your_test_key_here
RAZORPAY_KEY_SECRET=your_test_secret_here

# ShipRocket Shipping Integration
# Get credentials from ShipRocket Dashboard
SHIPROCKET_EMAIL=your_shiprocket_email@example.com
SHIPROCKET_PASSWORD=your_shiprocket_password

# Logging (Optional)
LOG_LEVEL=info
LOG_DIR=./logs
